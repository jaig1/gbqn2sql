# Google Cloud Configuration
# ========================

# Your Google Cloud Project ID
GCP_PROJECT_ID=your-project-id

# Service Account for BigQuery access (with impersonation)
GOOGLE_CLOUD_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com

# BigQuery Dataset Configuration
BQ_DATASET_ID=insurance_analytics
BQ_LOCATION=US

# Vertex AI Configuration
VERTEX_AI_LOCATION=us-central1
GEMINI_MODEL_NAME=gemini-2.5-flash-lite

# Optional: Streamlit Configuration
STREAMLIT_PORT=8501

# Setup Instructions:
# ===================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual Google Cloud settings
# 3. Ensure your service account has the following roles:
#    - BigQuery Admin
#    - Vertex AI User
#    - Service Account Token Creator (for impersonation)
# 4. Run: gcloud auth application-default login
# 5. Run: python3 setup.py